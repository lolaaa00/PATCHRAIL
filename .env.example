# Public — safe to expose to the browser bundle.
NEXT_PUBLIC_RELEASE_ADDRESS=
NEXT_PUBLIC_VAULT_ADDRESS=

# Server-side only, used solely by scripts/deploy.ts. NEVER commit a real
# value, NEVER prefix with NEXT_PUBLIC_, and never fund this key with more
# than deployment gas plus what you intend to test with.
PRIVATE_KEY=
